_site
.sass-cache
.jekyll-cache
.jekyll-metadata
# Alleen Rubys vendor/ in de root; assets/vendor/ (o.a. Leaflet) hoort in git
/vendor
.fonts.conf
.fontconfig-cache
__pycache__/
*.pyc
.venv/
.ruff_cache/
.playwright-mcp/

# Unpublished drafts, never checked in. Jekyll skips _drafts unless
# --drafts is passed, and check_links.py only scans _posts.
_drafts/

# Rendered by `just cv` for a local preview only. On the live site both files
# are produced by .github/workflows/deploy.yml; they are never committed.
/assets/cv-anne-schuth.pdf
/assets/cv-thumbnail.png
